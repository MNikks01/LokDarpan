# Fingerprints gitleaks should not report.
#
# Each entry pins one commit + file + rule + line, so it can never suppress a
# future finding — only the exact historical occurrence named.
#
# 6fd0c7a: a synthetic, Google-API-key-shaped fixture in the credential-scrubber
# tests. The scanner was correct to flag it; the fixture was replaced with an
# obvious placeholder in the following commit. This entry covers only the
# superseded commit, which disappears from history when the branch is
# squash-merged — at which point this file can be emptied.
6fd0c7a61a9c740c963e1d050b0e956ae3c5566d:packages/observability/tests/scrub.test.ts:generic-api-key:34
